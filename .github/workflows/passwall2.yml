#=================================================
# https://github.com/wukongdaily/RunFilesBuilder
# Description: Build RunFiles using GitHub Actions
# Lisence: MIT
# Author: wukongdaily
# Blog: wkdaily.cpolar.cn
#=================================================

name: NEW Make Passwall2 run files

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write  # 赋予发布 Release 的写权限

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Fetch latest release tag from Passwall2
        id: fetch_latest_tag
        run: |
          API_URL="https://api.github.com/repos/Openwrt-Passwall/openwrt-passwall2/releases/latest"
          RESPONSE=$(curl -sL "$API_URL")
          
          latest_tag=$(echo "$RESPONSE" | jq -r '.tag_name')
          
          if [ "$latest_tag" == "null" ] || [ -z "$latest_tag" ]; then
            echo "Error: Failed to fetch latest tag."
            echo "API Response: $RESPONSE"
            exit 1
          fi
          
          echo "LATEST_TAG=$latest_tag" >> $GITHUB_ENV
          echo "Fetched tag: $latest_tag"

      - name: Clone makeself repository
        run: git clone https://github.com/megastep/makeself.git

      - name: Download latest Passwall2 files
        run: |
          mkdir -p downloads
          cd downloads
          
          echo "Fetching Release JSON..."
          RELEASE_JSON=$(curl -sL "https://api.github.com/repos/Openwrt-Passwall/openwrt-passwall2/releases/latest")
          
          # 获取所有匹配 luci-*-passwall2*.ipk 的文件 (主程序+语言包)
          URL_LUCI=$(echo "$RELEASE_JSON" | jq -r '.assets[] | select(.name | test("luci-.*-passwall2.*\\.ipk")) | .browser_download_url')

          # 检查是否有文件
          if [ -z "$URL_LUCI" ]; then
            echo "Error: Failed to extract any download URLs."
            exit 1
          fi

          echo "Downloading LuCI packages..."
          for url in $URL_LUCI; do
            if [ ! -z "$url" ] && [ "$url" != "null" ]; then
                curl -LO "$url"
            fi
          done
          
          ls -lh

      - name: Organize files (LuCI Only)
        run: |
          # 因为没有核心包，这里只打包 LuCI ipk
          mkdir -p passwall_luci/main
          cp downloads/luci-*.ipk passwall_luci/main/

      - name: Create install.sh scripts
        run: |
          cat <<EOF > passwall_luci/install.sh
          #!/bin/sh
          echo "Installing Passwall2 LuCI..."
          # 安装主程序 (语言包通常会自动作为依赖安装，或者通过通配符一起安装)
          if ls main/*.ipk 1> /dev/null 2>&1; then
            opkg install main/*.ipk
          fi
          echo "Installation Complete. Note: Core packages (xray/sing-box) are NOT included in this runfile."
          EOF
          
          chmod +x passwall_luci/install.sh

      - name: Move directory to makeself
        run: |
          mv passwall_luci makeself/

      - name: Create self-extracting package
        run: |
          cd makeself
          ./makeself.sh passwall_luci/ passwall2_luci_${{ env.LATEST_TAG }}.run "Passwall2 LuCI Installer" ./install.sh

      - name: Check file sizes
        run: |
          ls -lh makeself/passwall2*.run

      - name: Preparing release name
        run: |
          release_name=$(TZ="Asia/Shanghai" date +'%Y-%m-%d %H:%M Build')
          echo "RELEASE_NAME=$release_name" >> $GITHUB_ENV

      - name: Fetch latest release details
        id: fetch_release_details
        run: |
          extra_content="![Github](https://img.shields.io/badge/Passwall2.run-123456?logo=github&logoColor=fff&labelColor=green&style=for-the-badge) [![Github](https://img.shields.io/badge/国内加速站下载-FC7C0D?logo=github&logoColor=fff&labelColor=000&style=for-the-badge)](https://wkdaily.cpolar.cn/archives/1) ![GitHub Downloads (all assets, specific tag)](https://img.shields.io/github/downloads/wukongdaily/RunFilesBuilder/${{ env.LATEST_TAG }}/total?style=for-the-badge&labelColor=black&color=%2325c2a0)"
          
          release_notes=$(curl -sL https://api.github.com/repos/Openwrt-Passwall/openwrt-passwall2/releases/latest | jq -r '.body')
          
          if [ -z "$release_notes" ] || [ "$release_notes" == "null" ]; then
            release_notes="No release notes available."
          fi
          echo -e "$extra_content\n\n$release_notes" > release_notes.md

      - name: Release and Upload Assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.LATEST_TAG }}
          name: "PassWall2-${{ env.LATEST_TAG }}"
          body_path: release_notes.md
          files: makeself/passwall2*.run
          token: ${{ secrets.GITHUB_TOKEN }}
